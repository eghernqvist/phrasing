.alert
  %h1 You're in production!
  - unless Phrack.staging_server_endpoint.nil?
    %p You should do your changes in #{link_to("staging", Phrack.staging_server_endpoint.gsub("/download", ""))} and then #{link_to("sync them",sync_phrack_phrases_path)}.
